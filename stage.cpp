// A stage has a layout of blocks that are destroyed when the ball hits them

#include "stage.h"
#include "ofMain.h"

Stage::Stage() {
	blocks = 0;
}

Stage::Stage(int blocks) {
	this->blocks = blocks;
}

void Stage::spawnBlocks(int level) {
	if (level == 1) {

	}
	else if (level == 2) {

	}
	else if (level == 3) {

	}
	else if (level == 4) {

	}
}